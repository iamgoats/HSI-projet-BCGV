#ifndef APP_DATA_H
#define APP_DATA_H

#include <stdint.h>
#include <string.h>

// Types définis

// Structure des données applicatives
typedef struct {
} AppData;

// Prototypes des fonctions
void init_app_data(AppData *data);

#endif // APP_DATA_H
